# Archived translator helpers

This directory contains the former register based load/store translator helpers. The active ARM64 translator uses stack based load/store paths and the atomic bridge in `internal/arch/arm64`; these helpers have no active call sites.

The sources are preserved for history and are not maintained or built as part of the root Go module.
