# Archived development plans

This directory preserves historical OMX planning and context artifacts for provenance only.

The files under `context/` and `plans/` are not active product source, release requirements, or current implementation guidance. Current product behavior and release gates are defined by the active source tree, tests, `docs/`, and the canonical Verification workflow.
