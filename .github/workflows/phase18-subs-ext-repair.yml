name: Phase18 SUBS extended repair

on:
  push:
    branches:
      - fix/compiler-r29-coverage-v2
    paths:
      - .github/workflows/phase18-subs-ext-repair.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: fix/compiler-r29-coverage-v2
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
      - name: Apply audited SUBS extended exclusive repair
        run: |
          set -euo pipefail
          python3 scripts/phase18_subs_ext_repair.py
          gofmt -w internal/arch/arm64/tr_exclusive.go internal/arch/arm64/exclusive_compiler_r29_test.go
          go test -count=1 ./internal/arch/arm64
          go vet ./internal/arch/arm64
      - name: Commit product-only result
        run: |
          set -euo pipefail
          git config user.name 'vmpacker-phase18-bot'
          git config user.email 'actions@users.noreply.github.com'
          git rm .github/workflows/phase18-subs-ext-repair.yml scripts/phase18_subs_ext_repair.py
          git add internal/arch/arm64/tr_exclusive.go internal/arch/arm64/exclusive_compiler_r29_test.go
          git diff --cached --check
          git commit -m 'Support SUBS extended in exclusive thunks'
          git push origin HEAD:fix/compiler-r29-coverage-v2
